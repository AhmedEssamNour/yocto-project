# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-custom-updates"
BBFILE_PATTERN_meta-custom-updates = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-custom-updates = "6"

LAYERDEPENDS_meta-custom-updates = "core"
LAYERSERIES_COMPAT_meta-custom-updates = "wrynose"
